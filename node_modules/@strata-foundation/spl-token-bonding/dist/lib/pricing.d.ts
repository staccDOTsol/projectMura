import { PublicKey } from "@solana/web3.js";
import { BondingHierarchy } from ".";
export declare class BondingPricing {
    hierarchy: BondingHierarchy;
    constructor(args: {
        hierarchy: BondingHierarchy;
    });
    current(baseMint?: PublicKey): number;
    locked(baseMint?: PublicKey): number;
    swap(baseAmount: number, baseMint: PublicKey, targetMint: PublicKey): number;
    swapTargetAmount(targetAmount: number, baseMint: PublicKey, targetMint: PublicKey): number;
    sellTargetAmount(targetAmountNum: number, baseMint?: PublicKey): number;
    buyTargetAmount(targetAmountNum: number, baseMint?: PublicKey): number;
    buyWithBaseAmount(baseAmountNum: number, baseMint?: PublicKey): number;
}
//# sourceMappingURL=pricing.d.ts.map